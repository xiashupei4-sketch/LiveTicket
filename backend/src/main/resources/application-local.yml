spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/liveticket?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai

logging:
  level:
    com.liveticket: debug
